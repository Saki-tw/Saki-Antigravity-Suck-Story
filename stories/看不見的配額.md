# 看不見的配額 💀💀

> *「DOM 裡什麼都沒有...」*
> — 最後搜索時間：2026-01-26 14:40

---

## 第一幕：平靜的開始

我需要顯示配額。

這應該很簡單，對吧？只要從 UI 裡抓取數字就好。

我寫了一個 DOM 搜索腳本，搜索所有可能包含 `quota`、`usage`、`remaining` 的元素。

```javascript
const walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT);
// 搜索所有配額相關文字...
```

然後我執行了它。

---

## 第二幕：不對勁的徵兆

結果回來了。

```json
{ "ok": false, "found": [] }
```

什麼都沒有。

我不信。我擴大搜索範圍。搜索所有百分比符號。

```json
{ "count": 0, "samples": [] }
```

**零個結果。**

這不可能。我明明看到畫面上有配額顯示。
我每天都在看它。
它就在那裡。

**但 DOM 裡什麼都沒有。**

---

## 第三幕：恐怖的真相

我開始在網路上搜索。找到了一些「Antigravity Quota」的擴充套件 repo。

它們告訴我真相：

> *「配額不在 DOM 裡。」*
> *「它在 Language Server 的 API 裡。」*

我翻閱它們的源碼，發現了這個：

```
/exa.language_server_pb.LanguageServerService/GetUserStatus
```

配額從來就不在 UI 裡。
我一直在搜索**一個不存在的東西**。

那些在畫面上閃爍的數字——它們是**幻影**。
它們來自 API，經過渲染，顯示在我眼前。
但它們從未真正存在於 DOM 之中。

我所做的一切 DOM 搜索，都只是在黑暗中摸索。

---

## 結語：倖存者的警告

如果你需要從 Antigravity 獲取資訊：

1. **不要**相信你看到的 UI
2. **不要**嘗試從 DOM 抓取
3. **去找** Language Server 的 API

因為 UI 只是**幻象**。
真正的數據，藏在你看不見的地方。

---

*「眼見不一定為實。*
*在 Electron 的世界裡，*
*DOM 只是冰山一角。」*

---

**恐怖程度：** 💀💀

**關鍵字：** `DOM`, `GetUserStatus`, `Language Server`, `幻影`

**發生時間：** 2026-01-26 14:00-14:40

**倖存狀態：** ✅ 已找到正確 API
